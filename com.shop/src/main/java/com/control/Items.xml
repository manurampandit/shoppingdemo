<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Items">
	
	<select id="getItemList" resultType="Items">  
		SELECT  ITEMID itemId,ITEMNAME itemName, PRICE price,PICPATH picPath
		FROM ITEMS; 
	</select>
	
	<select id="getItemDetails" parameterType="int" resultType="Items">
		SELECT  ITEMID itemId,ITEMNAME itemName, PRICE price, PICPATH picPath
		FROM ITEMS
		WHERE
		ITEMID = #{itemId};
	</select>
	
</mapper>